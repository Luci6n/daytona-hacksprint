//
//  ContentView.swift
//  DaddyFix
//
//  Temporary shell so Brian can run AR on device immediately.
//  Lucian owns the final ContentView / AppState / voice / payments —
//  replace this with the full UI and embed ARViewContainer.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        ARDebugHostView()
    }
}

#Preview {
    ContentView()
}
